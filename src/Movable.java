/**
 * Holds all the methods that decides how the car is moving. It also forces the class that implements the interface to also implement all the methods in it.
 */

public interface Movable {
    
    void move();

     void turnLeft();

     void turnRight();
}
